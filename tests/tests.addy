import "test1.addy";

# Input
text inp = sys.in("Press enter to run all tests, or enter the number of tests you want to execute: ");
number test = -1;
if(inp != "") {
	tests = inp.toNum();
}

# Test 1
comp.run(test1);